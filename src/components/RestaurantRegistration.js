import React from "react";
import FormWrap from "./FormWrap";

function RestaurantRegistration(props) {
  return (
    <div className="RestaurantRegistration">
      <pre className="SearchResultsFound RestaurantRegistrationHistory">
        Home / Restaurant Registration
      </pre>
      <FormWrap />
    </div>
  );
}

export default RestaurantRegistration;
